import { PopoutMenuDirective } from './popout-menu.directive';

describe('PopoutMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoutMenuDirective();
    expect(directive).toBeTruthy();
  });
});
